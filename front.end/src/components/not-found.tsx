import * as React from 'react';
import { StatelessComponent } from 'react';

const NotFound: StatelessComponent<any> = (): any => {
  return (
    <div>The page you request does not exist.</div>
  );
};

export default NotFound;
